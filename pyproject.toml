[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "clinical-deterioration-ai"
version = "0.1.0"
description = "Clinical deterioration risk prediction package"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
